import ReactDOM from 'react-dom/client';
import CropScreen from 'src/renderer/components/CropScreen';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(<CropScreen />);
