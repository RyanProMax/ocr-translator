export enum Pages {
  Home = 'home.html',
  CropScreen = 'cropScreen.html',
  Capture = 'captureScreen.html',
}

export enum Channels {
  CreateWindow = 'CreateWindow',
  CropScreen = 'CropScreen',
  CropScreenConfirm = 'CropScreenConfirm',
  CropScreenCancel = 'CropScreenCancel',
  Quit = 'Quit',
}
